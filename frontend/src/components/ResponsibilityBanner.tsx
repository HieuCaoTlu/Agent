/** GD-7 (Plan.MD 11.2) — banner cố định ở chân màn hình nhắc trách nhiệm đối chiếu. */
export function ResponsibilityBanner() {
  return (
    <div className="sticky bottom-0 border-t border-amber-300 bg-amber-50 px-4 py-2 text-center text-base text-amber-900">
      ⚠ Đây là GỢI Ý của AI. Cán bộ phải đối chiếu giấy tờ gốc trước khi dùng.
    </div>
  )
}
