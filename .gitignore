.env
.venv/
__pycache__/
*.pyc
.uv/
uv.lock

# Log hội thoại và PDF nguồn tải lên — dữ liệu runtime/nhạy cảm, không commit.
# data/index.json và data/procedures/ (đã parse) vẫn được track vì app cần
# chúng để chạy được ngay sau khi clone, không phải build_index.py lại từ đầu.
data/logs/
data/pdfs/
data/allowed_users.json

# Dữ liệu khảo sát form thật (scripts/inspect_form_live.py) — network log/DOM
# snapshot có thể chứa thông tin cá nhân thật (họ tên, số định danh...) của
# người đăng nhập lúc khảo sát, không commit.
data/form_inspect/
data/eform_*_apiid_scan.json
data/eform_*_apiid_scan.progress
